# CAS Implementation Plans — Index

| Date | Plan | Status |
|---|---|---|
| 2026-07-08 | [Customer PO required](2026-07-08-customer-po-required.md) | **Ready — not started.** R-01. Per-customer `po_required` boolean (default False) on Customer + batch migration; a "Requires Purchase Order" checkbox on the customer form (create/edit persist); a guard in `sales_orders.confirm` blocking draft→confirmed when `so.customer.po_required` and the SO's `customer_po_number` is blank. PO number only, confirm-only. 3 tasks, TDD. Spec: `specs/2026-07-08-customer-po-required-design.md`. Branch `feat/so-product-based-lines`. |
| 2026-07-08 | [Salesperson attribution](2026-07-08-salesperson-attribution.md) | **Ready — not started.** R-01. Nullable `salesperson_id` FK→`employees` on SO + SI headers (one batch migration), Employee `<select>` on both forms gated on the Employees module, salesperson name on SI print/detail/pre-printed. `copy_salesperson` helper stubbed. DR field + SO→DR→SI auto-fill cascade DEFERRED (unbuilt chain). 4 tasks, TDD. Spec: `specs/2026-07-08-salesperson-attribution-design.md`. Branch `feat/so-product-based-lines`. |
| 2026-07-08 | [Order Monitoring dashboard](2026-07-08-order-monitoring.md) | **Ready — not started.** R-01 #8. Count-based, no-monetary, branch-scoped read-over of `SalesOrder`: new `app/sales_orders/monitoring.py` pure `get_order_monitoring(branch_id, today)`, `sales_orders.monitor` route + `monitoring.html` (4 count cards + Chart.js by-status donut + aging bar + top-customers table, reusing bundled `chart.umd.min.js`), additive `overdue`/`due_soon` SO-list filters for drill-through, list-header entry link. No migration. 3 tasks, TDD. Spec: `specs/2026-07-08-order-monitoring-design.md`. Branch `feat/so-product-based-lines` (R-01 lineage). |
| 2026-07-08 | [Sales Order — product-based lines](2026-07-08-so-product-based-lines.md) | **Ready — not started.** R-01. SO becomes a product-based operational doc: hard-drop `SalesOrderItem.description` (batch migration), require a Product per line via a server-side guard, gate the `sales_orders` module on Products (`optional:True, depends_on:['products'], default_enabled:False, per_user:True` + `all_permission_keys` per-user decoupling), product-first form/detail/print, delete orphan `view.html`. 4 tasks, TDD. Spec: `specs/2026-07-08-so-product-based-lines-design.md`. Branch `feat/so-product-based-lines` (stacked on the SO 500-fix + display-fix). |
| 2026-07-08 | [EOM reporting date + two-column IS & CF](2026-07-08-eom-reporting-date-is-cf.md) | **Ready — not started.** All 4 financial statements default their reporting date to end of current month; Income Statement & Cash Flow become reporting-date-driven two-column (Current-Month + Year-to-Date). New `end_of_month` util, `_stmt_params`, `merge_is_two_column`/`merge_cf_two_column` (`app/reports/two_column.py`); templates/Excel/print gain a 2nd column; `_is_params` retired. 10 tasks, TDD. Spec: `specs/2026-07-08-eom-reporting-date-is-cf-design.md`. Origin: alvinccruz balance-sheet audit found a mid-month cutoff distortion. |
| 2026-07-08 | [Employee master + combined payee dropdown](2026-07-08-employee-master-combined-payee.md) | **Ready — not started.** Payroll arc slice 1. Opt-in branch-scoped Employee master (`app/employees/`, Payroll area) + polymorphic payee on `AccountsPayable` (`payee_type`/`payee_id`, `vendor_id` nullable) so one AP form dropdown lists vendors+employees, with employee-payee vouchers segregated from AP aging/BIR purchases/supplier Alphalist. 12 tasks / 4 phases. Spec: `specs/2026-07-08-employee-master-design.md`. Party model (one person, many roles) deferred. |
| 2026-07-07 | [Clean copy — firm+software COA + seed-firm](2026-07-07-firm-software-coa-clean-copy.md) | **DONE + DEPLOYED** — `flask seed-firm` + `FIRM_COA` (116 accts), 17 tests; pushed; LIVE at alvinccruz.pythonanywhere.com on fresh cas.db (SavorPack demo archived) |
| 2026-07-03 | [RIC legacy COA import](2026-07-03-ric-coa-import.md) | Done — imported to `ric.db` (391 accts); final review YES; on main, not pushed |
| 2026-07-03 | [RIC COA reconciliation (seed retirement)](2026-07-03-ric-coa-reconciliation.md) | Done — executed on `ric.db` (391→368); D1–D4 resolved; scripts/ric_coa/reconcile.py |
| 2026-07-03 | [WHT per-rate posting routing](2026-07-03-wht-per-rate-posting.md) | Done — final review YES; AP+CDV bucket WHT payable by ATC (20301 fallback); on main, not pushed |
| 2026-07-03 | [Opening Balances — SI line-item parity](2026-07-03-opening-balances-si-parity.md) | Done (on main, not pushed) |
| 2026-07-03 | [Transaction-form line-item component standard](2026-07-03-transaction-line-item-standard.md) | Scoped — not started |
| 2026-07-03 | [Food Toll Packing demo dataset](2026-07-03-food-toll-packing-demo.md) | Done (on main, not pushed) |
| 2026-07-04 | [CDV check printing + per-account layout](2026-07-04-cdv-check-printing.md) | **SUPERSEDED** by 2026-07-07-cdv-check-writer (targets a non-existent FPDF/PrintLayout engine) |
| 2026-07-07 | [CDV check writer](2026-07-07-cdv-check-writer.md) | **Ready — Phase 0 gate open; Phase 1 not started.** Overprint a check-payment CDV onto bank stock via the real HTML-canvas engine. Boardroom-decided 2026-07-07: **PDF via fpdf2** for the print (browser `@page` can't register), key per **`cash_account_id`** (real FK now), **DB partial-unique serial index** `(cash_account_id, check_number)` in v1, `amount_to_words` TDD-first w/ an independent spell↔parse oracle, no facsimile signature, date MM-DD-YYYY. ~4.5–5 dev-days; gated on RIC bank confirming machine-printed words (now de-risked by PCHC research). Supersedes the 2026-07-04 spec+plan. |
| 2026-07-05 | [SI pre-printed layout designer](2026-07-05-si-preprinted-layout-designer.md) | **Done (on main, not pushed)** — shipped + extended well beyond the plan: drag/resize/duplicate fields, independent column positioning, show/hide fields+columns, per-element + band font/bold, grouped dot-matrix fonts, paper toggle (continuous/Letter), date-format picker, BIR summary fields, editable Preparer/Checker/Approver texts, value-only output, margin guides, `@page` margin:0. Layout JSON in `app_settings`; ~50 tests (unit+integration+Playwright e2e) |
